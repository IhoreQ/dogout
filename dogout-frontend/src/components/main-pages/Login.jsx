import React from "react";
import MainBox from "../MainBox";

const Login = () => {

    React.useEffect(() => {
        document.title = 'Login | DogOut';
      }, []);

    return (
        <MainBox>

        </MainBox>
    )
}

export default Login;